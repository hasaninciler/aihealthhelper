## Kurulum ve Çalıştırma

Bu projede API anahtarının güvenli bir şekilde kullanılabilmesi için
istemci (client) tarafının yanı sıra bir **sunucu (server)** yapısı da
kullanılmaktadır. Projeyi çalıştırmak için aşağıdaki adımların sırasıyla
uygulanması gerekmektedir.

### 1. Gerekli bağımlılıkların yüklenmesi
```bash
npm install
node server.js

direkt index.html'i çalıştırıp test edebilirsiniz